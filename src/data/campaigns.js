// Campaigns are now managed via Decap CMS.
// Edit at /admin on your live site, or directly in src/data/campaigns.json.
import data from './campaigns.json';
export const campaigns = data.campaigns;

